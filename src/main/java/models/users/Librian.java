package models.users;

public class Librian extends User {
    public Librian(int id, String firstName, String lastName, String username, String password, String role) {
        super(id, firstName, lastName, username, password, role);
    }
}
